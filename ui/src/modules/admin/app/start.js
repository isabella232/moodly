/** @jsx React.DOM */

var React = require("react");

var Start = function(moodlyBackend) {
    React.createClass({
        render: function () {
            return (
                <div style="text-align:center">
                    <h1>Hurra</h1>
                </div>
                );
        }

    });
};

module.exports = Start;
